// 教学：!= 不自动取反，需补运算符 函数 !=（第 20 章 §20.4.2）
类 向量 {
    公开 让 横向: 整数
    初始化(横向: 整数) { 本对象.横向 = 横向 }
    公开 运算符 函数 ==(其他: 向量): 布尔 { 返回 true }
}
主函数() {
    让 甲 = 向量(1)
    让 乙 = 向量(1)
    打印行("${甲 != 乙}")
}
