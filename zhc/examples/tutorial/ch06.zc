// 教程 06 错误处理验收示例（选项/匹配/尝试捕获/最终）：zhc run ch06.zc
函数 找名字(编号: 整数): 选项<字符串> {
    如果 (编号 == 1) { 返回 有值("小明") }
    返回 无值
}

函数 除以(被除数: 整数, 除数: 整数): 整数 {
    如果 (除数 == 0) {
        抛出 异常("除数不能为零")
    }
    返回 被除数 / 除数
}

主函数() {
    匹配 (找名字(2)) {
        情况 有值(名字) => 打印行("找到了：${名字}")
        情况 无值 => 打印行("没有这个人")
    }
    尝试 {
        让 结果 = 除以(10, 0)
        打印行("结果：${结果}")
    } 捕获 (错误: 异常) {
        打印行("捕获到异常：${错误.message}")
    } 最终 {
        打印行("无论成败都会执行")
    }
}
