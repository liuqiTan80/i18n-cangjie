// 한국어 방언 예제 (.kc 확장자로 언어 자동 판별 — ZHCLANG 불필요)
// 참고: 메인 → main. 공식 Cangjie와 동일하게 main 선언에 func를 붙이지 않음 (中文「主函数」과 동일)
메인() {
    let 이름: 문자열 = "한국"
    출력("안녕하세요, ${이름}!")

    // 타입 별칭: 정수 → Int64
    let 숫자: 정수 = 42
    출력("숫자: ${숫자}")
}
