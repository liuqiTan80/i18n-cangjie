// 日本語方言の例（.jc 拡張子で自動判定 — ZHCLANG 不要）
// 注意：メイン → main。公式と同じく main 宣言に func は付けません。
メイン() {
    おく 名前: 文字列 = "日本"
    ひょうじ("こんにちは、${名前}！")

    // 型エイリアス：せいすう → Int64
    おく 数: せいすう = 42
    ひょうじ("数: ${数}")
}
