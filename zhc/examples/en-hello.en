// English dialect example (run with ZHCLANG=en):
// the en pack is an identity mapping — English dialect == official Cangjie.
// Note: main() has no func keyword.
main() {
    let name: String = "English"
    println("Hello, ${name}!")
}
