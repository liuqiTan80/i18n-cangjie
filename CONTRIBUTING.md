# 贡献指南（CONTRIBUTING）

欢迎！zhc 是「用母语学仓颉」的教学工具链：语言包把标准仓颉变成中文方言，
配套整本《中文仓颉程序设计》教程、错误字典与 IDE 扩展。任何让「中文编程
教学闭环」更完整的小改动都值得提——先读本指南，再动手。

## 项目结构速览

| 目录 | 内容 | 改前必读 |
|---|---|---|
| `zhc/` | zhc 工具本体（Cangjie 源码，34 文件） | 改动必须实测（cjpm test）+ 全量验收 |
| `docs/中文仓颉程序设计/` | 19 章教程（三卷 + 附录）——核心资产 | 代码块改后必须跑教程验证 |
| `docs/` | 术语表 / 错误字典 / 语言包开发指南 / 选书指南 | 用词先查[术语表](docs/术语表.md) |
| `zhc/lang-packs/` | 语言包（zh 完整 / en 恒等 / ru 演示） | [语言包开发.md](docs/语言包开发.md) |
| `tools/` | 生成器（错误字典/高亮/语言包）与 VS Code 扩展 | 生成物防漂移门禁 |
| `scripts/` | 验收 / 教程验证 / 发布 / 安装脚本 | 新脚本进 acceptance 段 11 |

## 开发环境与验证（提交前必须全过）

```bash
cd zhc && cjpm build                 # 构建（需仓颉 SDK，见 scripts/setup-cangjie.sh）
cd .. && bash scripts/acceptance.sh  # 一键全量验收（本地与 CI 同一入口）
```

- **改教程正文/代码**：`bash scripts/tutorial-check.sh --refresh`（正文代码有意修改时
  更新转译快照基线，随改动一并提交）；
- **改语言包**：`zhc mapping check` + 教程验证（教程跑不过 = 词表缺词）；
- **改错误翻译**：改 `tools/gen_full_errors.py` 的 `CURATED` 或
  `tools/diag_translations*.py` 后**重新生成**，不得手改生成物；
- **任何改动**：全量 `bash scripts/acceptance.sh` 通过后再提 PR。

## 代码约定

- 注释用中文；术语先查[术语表](docs/术语表.md)（避免一词多译）；
- Cangjie 源码风格参照既有文件（模块头设计注释、§引用、实测结论记录）；
- 教程代码须通过 `zhc lint --style`（全角/行长/CRLF 排版门禁）与串行实测；
- 提交信息参照 `git log` 既有风格（动词开头，中文，可带审计/建议编号）。

## 提交流程

1. 先在 Issues 说明动机（bug 报告用模板，附复现与期望）；
2. 改代码 → 本地跑完验收 → 提交（小步、可独立验证）；
3. PR 描述：改动摘要 + 验收结果（acceptance 通过数）+ 是否 refresh 快照；
4. 维护者会核对验收日志与生成物防漂移（段 13），无需贴大段输出。

## 新人入口（按投入从低到高）

1. **修订 631 条自动翻译中措辞生硬的条目**——单条改动小，最易入手
   （[语言包开发.md 第六节](docs/语言包开发.md)）；
2. **给教程补示例/思考题**——先跑 `check_langpack.py` 确认用词已被语言包覆盖；
3. **补 ru 语言包的 stdlib/errors**——完整度矩阵里唯一的「进行中」语言包；
4. **诊断码触发率报告**——`zhc check` 带 `ZHC_DIAG_STATS` 收集实战命中码，
   0 命中清单即精翻候选（见 [语言包开发.md](docs/语言包开发.md)）。

## 问题报告模板要点

- 环境：OS / 仓颉 SDK 版本 / zhc 构建方式（源码 or 离线包）；
- 复现：最小 `.zc` 文件 + 完整命令 + 实际输出 vs 期望输出；
- 归属：工具 bug / 词表缺词 / 教程错误 / 文档过期（不确定就写「待分类」）。
